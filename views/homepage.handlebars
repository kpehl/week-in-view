<!--<div>
    {{userData.username}}
    {{#each userData.events}}
    <h3>{{ title }}</h3>
    <p>{{ text }}</p>
    <p>{{hour}}:{{minute}}, {{month}}/{{day}}/{{year}}</p>
    {{/each}}
</div> -->

<div class = "container-fluid">
    <div class = "row">
        <div class = "col-9">
            {{> calendar }}
        </div>

        <div class = "col-3">
            <h3>Tasks</h3>
            <ul class = "task-list">
                {{#each userData.tasks as |task|}}
                <li>
                    {{> tasks}}
                </li>
                {{/each}}
            </ul>
            <form class = "new-task-form">
                <div>
                    <input name = "task-text" />
                </div>
                <div>
                    <button type="submit">Add Task</button>
                </div>
            </form>
        </div>
    </div>
</div>



{{> weather }}


<script src="/javascript/add-task.js"></script>